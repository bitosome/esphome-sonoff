# Allow importing this package
#dashboard_import:
  #package_import_url: "github://bitosome/esphome/sonoff-m5/indicator-led.yaml@main"
  #import_full_config: false

switch:
  # Physical indicator of the relay
  - platform: gpio
    id: led_indicator
    internal: true
    pin: ${led_indicator_gpio}